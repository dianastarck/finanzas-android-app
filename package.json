{
  "name": "mi-ahorro-app",
  "version": "1.0.0",
  "description": "App Android nativa con Capacitor para el MVP de finanzas personales conectado a Supabase.",
  "scripts": {
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:assets": "npx capacitor-assets generate --android",
    "android:debug": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:release:aab": "npm run android:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/assets": "latest"
  }
}
